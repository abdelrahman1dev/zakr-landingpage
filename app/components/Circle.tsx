import React from 'react'

function Circle({className}: {className?: string}) {
  return (
         <span className={`absolute bg-primary ${className}`}>

          </span>
    
  )
}

export default Circle
